# Variáveis de Ambiente

## Backend `medis-back`

Variáveis já conhecidas:

- `DATABASE_URL`
- `JWT_SECRET`
- `PREDICTION_API_URL`

## Frontend `medis-front`

Estado atual conhecido:

- URLs de integração estão fixas em código
- externalização para variáveis de ambiente ainda não está consolidada

## Serviço `medis-modelos`

Variáveis ainda precisam ser mapeadas e formalizadas.

## Próximos detalhamentos

- tabela completa por repositório
- obrigatória vs opcional
- valores padrão de desenvolvimento
